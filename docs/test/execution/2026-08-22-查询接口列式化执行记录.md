# 查询接口列式化执行记录

日期：2026-08-22   环境（版本/分支）：main 工作区；server jar 22:43 构建；astradb-client 22:43 install 到 .m2-repository
关联用例：[2026-08-22-查询接口列式化测试用例.md](../archive/cases/2026-08-22-查询接口列式化测试用例.md)

## 执行结果

| 用例ID | 用例标题 | 结果 | 实际结果 | 缺陷ID | 备注 |
|---|---|---|---|---|---|
| QC-1 | getSnapshot 列式结构 | 通过 | pk=id、columns=[id,v,region]、rows 每行 [key,v,region]（主键首位）、null 还原、totalRows/offset/limit 存在 | — | NullableColumnDeliveryTest.nullableColumnRoundtrip |
| QC-2 | getFullSnapshot 列式流式结构 | 通过 | pk/columns 前置、rows 二维数组 3 行、null 还原、totalRows=3 | — | IntegrityTest.itSnapshotRoundtrip |
| QC-3 | getPointSeries rows+timestamps 对齐 | 通过 | rows.size=3、timestamps 与行对齐（TS 递增）、末行最近值、未知 key 空 rows | — | IntegrityTest.itPointSeries + NullableColumnDeliveryTest.nullableMixedAcrossSnapshots + ScheduledImportTest.smoke |
| QC-4 | client queryPointAt 新格式解析 | 通过 | 含引号/换行/制表符 key 经 client 导入后 queryPointAt 可查且值正确（install 新 client 后） | — | LatestServerDeliveryTest.clientSpecialCharKeyIngest + ScheduledImportTest.smoke 单点 |
| QC-5 | 二进制查询与 JSON 并存回归 | 通过 | 二进制 queryFullSnapshot 契约与 JSON 列式数据一致 | — | 黑盒 46 项整体回归（含 BinaryEndpoint 单测） |
| QC-6 | 前端渲染走查 | 通过 | browse 表头=响应 columns、rows 二维渲染（空值 ∅/esc）；searchPoint 用 rows 末行+timestamps 末值+跳过主键列 | — | table.html 代码走查 |

## 汇总

| 类型 | 通过 | 失败 | 阻塞 | 跳过 | 合计 |
|---|---|---|---|---|---|
| 黑盒 A | 5 | 0 | 0 | 0 | 5 |
| 走查 W | 1 | 0 | 0 | 0 | 1 |
| 合计 | 6 | 0 | 0 | 0 | 6 |

黑盒工程整体：**46 项通过**（Availability 7 + Integrity 8 + LatestDelivery 7 + LatestServer 8 + NullableColumn 6 + ScheduledImport smoke 1 + Security 9），BUILD SUCCESS。

## 执行中发现与处置（非产品缺陷）

- **O-1（测试环境）**：首轮黑盒 queryPointAt 失败——黑盒工程依赖 `.m2-repository` 中的 client jar，而新解析代码未 install，旧 client 用旧格式解析新响应 → 返回 null。处置：`mvn -pl client -am install` 刷新本地仓库后重跑全绿。**非被测代码缺陷，是测试环境依赖管理问题**（已写入计划风险项）。
- **O-2（测试范围）**：`ScheduledImportTest.fullTenMinuteScheduledImport`（10 分钟正式长测）本次跳过——其链路（spring 定时 + client 导入 + queryPointAt 单点 + getPointSeries 历史）已由 smoke 冒烟（同代码路径，7.5s）验证通过；正式长测待后续单独执行。

## 回归说明

- 全量单测 73 项（core 41 + client 16 + server 16）由应用工程师交付验证（BUILD SUCCESS），本次不重复执行；
- 黑盒 46 项覆盖三查询新格式、client SDK、二进制并存、安全/可靠性回归。
