# 可空列 UI 修复与压缩等级默认值测试执行记录

日期：2026-08-22   环境（版本/分支）：main（未提交工作区：server jar 2026-08-22 21:28 构建）
关联用例：[2026-08-22-可空列UI与压缩等级默认值测试用例.md](../archive/cases/2026-08-22-可空列UI与压缩等级默认值测试用例.md)
执行方式：黑盒（test/ 工程 `NullableColumnDeliveryTest`，真实 server 进程）+ 渲染（curl）+ 前端代码走查

## 执行结果

| 用例ID | 用例标题 | 结果 | 实际结果 | 缺陷ID | 备注 |
|---|---|---|---|---|---|
| UI-N1 | 可空列 CSV 空字段导入 → null 还原 | 通过 | 3 行导入成功；行1 v=null/region=east、行2 v=1.5/region=null、行3 全非空正确 | — | 自动化 `nullableColumnRoundtrip` |
| UI-N2 | 非空列空字段导入 → 400 拒绝、无数据 | 通过 | 400 INGEST_REJECTED；getTableStats totalRows=0 | — | 自动化 `nonNullableColumnRejectsEmpty` |
| UI-N3 | 主键列 nullable:true 建表 → 400 | 通过 | 400"主键列不允许为可空"；listTables 无该表 | — | 自动化 `primaryKeyNullableRejected` |
| UI-N4 | 可空列混合数据跨快照正确 | 通过 | 点1 历史 [null, 9.0]、点2 历史 [1.0, null]，null 不串数据 | — | 自动化 `nullableMixedAcrossSnapshots` |
| UI-N5 | getTableInfo 返回 nullable/primaryKeyIndex | 通过 | schema.primaryKeyIndex=0；v/region nullable=true；id nullable=false | — | 自动化 `tableInfoSchemaForStructureDisplay` |
| UI-N6 | 建表显式 compressionLevel → TableInfo 一致 | 通过 | TableInfo.compressionLevel=10 | — | 自动化 `compressionLevelPersistsPerTable` |
| UI-C1 | GET / 渲染压缩等级默认值 3 | 通过 | HTML：`id="tlevel" type="number" value="3" min="1" max="22"` | — | curl 渲染（默认 env） |
| UI-C2 | ASTRA_DB_COMPRESSION_LEVEL=10 → 渲染 10 | 通过 | HTML：`value="10"`（同一输入框） | — | curl 渲染（env 覆盖实例） |
| UI-F1 | 复选框状态 → 请求体 nullable | 通过 | index.html:82 `nullable: cname===pk ? false : cnull.checked` | — | 代码走查 |
| UI-F2 | 主键列复选框禁用（syncPkNullable） | 通过 | index.html:65-71 tpk 联动禁用主键行 cnull 并清勾选 | — | 代码走查 |
| UI-F3 | 表结构展示 innerHTML + esc 转义 | 通过 | table.html:133-139 列/类型/主键✓/可空，name/type 经 esc()；数据来源 UI-N5 验证一致 | — | 代码走查 + UI-N5 |

## 汇总

| 类型 | 通过 | 失败 | 阻塞 | 跳过 | 合计 |
|---|---|---|---|---|---|
| 黑盒 A | 6 | 0 | 0 | 0 | 6 |
| 渲染 M | 2 | 0 | 0 | 0 | 2 |
| 走查 W | 3 | 0 | 0 | 0 | 3 |
| 合计 | 11 | 0 | 0 | 0 | 11 |

通过率：**100%**；产品缺陷：**0**

## 观察项（非缺陷，测试侧记录）

- **O-1（既有 API 契约）**：`POST /api/getSnapshot` 请求体缺 `offset`/`limit` 时返回 400 `UNPARSEABLE_BODY`（`SnapshotRequest.offset` 为必填 int）。前端浏览路径总是携带 offset/limit，本次测试首轮因用例输入不完整触发（测试侧修正）。非本次变更引入，建议文档/API 说明中明确 offset/limit 必填（可选改进）。
- **O-2（测试脚本）**：渲染验证首次执行时 `&&` 链与 `&` 组合导致 env 变量在子 shell 作用域失效、`$JAR` 为空报 JVM 参数错误——测试脚本问题，已改用独立脚本修正，不影响被测代码。

## 回归说明

- 被测 jar 为最新构建（含变更 1/2/3）；全量单测 73 项由应用工程师交付验证（本次不重复执行既有单测）。
- 既有黑盒资产（31 项）未受本次用例影响（独立表名 + 前后清理）。
