astradb:
  data-dir: ./data
  compression-level: 3
  # 按天分片时区（与页面/数据时间戳保持一致）；缺省取系统时区
  # 示例：Asia/Shanghai、UTC、+08:00
  timezone: Asia/Shanghai

server:
  port: 8080

spring:
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 200MB
  thymeleaf:
    cache: false
